						TODO:
							translate this to english
							criar um método validate (ht5ifv('validate') e ht5ifv('validate',nodes))
							adicionar um método para obter a lista dos elementos não válidos
							adicionar mensagens de texto ????
							adicionar a possibilidade de ter tipos definidos pelo utilizador (e porque não novas restrições?)  
							adicionar a possibilidade de redefinir os tipos  
							adicionar a possiblidade de desinibir a validação na submissão
							testes com campos desinibidos
							adicionar a possibilidade de testar campos alterados dinamicamente
							++adicinar expressões xpath/ccs3 para calcular automaticamente o valor de um campo
							associar diferentes eventos a diferentes verificações. Exemplo o blur a um required, pois o keyup permite abondanar um campo em branco 
							Só validar ao submeter ou pela chamada explicita ao método validator
							alterar o disable para re-iniciar sempre que hava uma alteração de um elemento de disable<->enable!Parece dificil


When the handler will be triggered is decided by the user (blur, keyup, ....)
Where the the handler signal the error will be defined by the users
What show be signaled is defined by the user
-------------------

Decide what will be shown in case of error
Decide where will be shown when there is an error
Decide when will be shown the error signal.
the plugin is quite harmeless

